class User {
    constructor(id, employee, district, username, password) {
        this.id = id;
        this.employee = employee;
        this.district = district;
        this.username = username;
        this.password = password;
    }
}

module.exports = User;
