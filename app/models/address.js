class Address {
    constructor(id, street, number, postalCode) {
        this.id = id;
        this.street = street;
        this.number = number;
        this.postal_code = postalCode;
    }
}

module.exports = Address;
